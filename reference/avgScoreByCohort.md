# Average Score by Cohort

This function takes a line level dataset of patient data and calculates
the average score for each cohort

## Usage

``` r
avgScoreByCohort(df)
```

## Value

OUTPUT_DESCRIPTION

## Details

DETAILS

## See also

[`group_by`](https://dplyr.tidyverse.org/reference/group_by.html),
[`summarise`](https://dplyr.tidyverse.org/reference/summarise.html)

## Examples

``` r
if (FALSE) { # \dontrun{
if(interactive()){
 #EXAMPLE1
 }
} # }
```
